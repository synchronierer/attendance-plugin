CREATE TABLE IF NOT EXISTS attendance_locations (id INTEGER PRIMARY KEY AUTOINCREMENT, code TEXT NOT NULL COLLATE NOCASE UNIQUE, name TEXT NOT NULL, type TEXT NOT NULL CHECK(type IN ('ARRIVAL','LOCATION')), active INTEGER NOT NULL DEFAULT 1 CHECK(active IN (0,1)), display_order INTEGER NOT NULL DEFAULT 0);
